pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                // Checkout the code from version control
                git 'https://github.com/sangeetha20dr/Project-1.git'
            }
        }
